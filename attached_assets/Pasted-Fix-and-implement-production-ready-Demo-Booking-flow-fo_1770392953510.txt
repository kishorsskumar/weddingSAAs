Fix and implement production-ready Demo Booking flow for Atbott SaaS.

IMPORTANT:
Currently:
- Demo booking shows success message
- Confirmation email not received
- Superadmin notification not received

We are NOT using WhatsApp.
Instead, implement internal Atbott AI Admin Notification System.

Do full debugging and implement end-to-end correctly.

--------------------------------------------------
STEP 1 — VERIFY DEMO FORM FLOW

Check:

• Form submission reaches backend
• Record is inserted into demo_bookings table
• API returns success only after DB insert
• Errors are not swallowed silently

Add structured logging for each stage.

--------------------------------------------------
STEP 2 — IMPLEMENT EMAIL CONFIRMATION (PRODUCTION READY)

Use Resend or SendGrid via backend API.

Trigger email ONLY after successful DB insert.

Email details:

Subject:
"Your Atbott Demo is Confirmed"

Body:

"Hi {{name}},

Your demo booking has been received.

Date: {{date}}
Time: {{time}}

Our team will contact you shortly.

– Team Atbott"

Requirements:

• Use backend route only
• Use ENV variable EMAIL_API_KEY
• Do NOT expose keys to frontend
• Handle errors safely
• Do NOT block user flow if email fails

Create table: email_logs

Fields:
- id
- recipient
- type (demo_confirmation)
- status (sent/failed)
- error_message (nullable)
- created_at

Log every email attempt.

--------------------------------------------------
STEP 3 — IMPLEMENT ATBOTT AI ADMIN NOTIFICATION SYSTEM

We are replacing WhatsApp with internal AI-based notification.

Create table: system_notifications

Fields:
- id (uuid)
- event_type (demo_booking)
- title (string)
- message (text)
- payload (JSON)
- is_read (boolean default false)
- created_at
- created_by (system)

When demo booking is successful:

Create notification:

Title:
"🚀 New Demo Booked"

Message:
"New demo booked by {{name}} from {{company}} on {{date}} at {{time}}."

Payload should include full booking data.

--------------------------------------------------
STEP 4 — REAL-TIME ADMIN ALERT

Implement real-time notification using:

• Supabase Realtime
OR
• WebSocket

When new notification is inserted:

• Superadmin sees bell icon badge increment
• Dropdown shows latest notification
• Mark as read functionality

--------------------------------------------------
STEP 5 — ATBOTT AI CHATBOT MESSAGE

If admin dashboard has AI chatbot panel:

Automatically insert AI message:

"New demo booked by {{name}} ({{company}}).
You may review and assign follow-up."

This message must:

• Appear in Superadmin chat panel
• Be saved in ai_chat_messages table
• Be tagged as system-generated

Do NOT send to normal users.
Only Superadmin role.

--------------------------------------------------
STEP 6 — ENTERPRISE AND SIGNUP EVENTS

Extend same notification system for:

New trial signup
Enterprise inquiry

Each event must:
• Create system notification
• Trigger AI chatbot message
• Log entry
• Not depend on external services

--------------------------------------------------
STEP 7 — ERROR HANDLING

If email fails:
• Log failure
• Show success page to user
• Do NOT crash flow

If notification insert fails:
• Log error
• Do NOT block booking

--------------------------------------------------
STEP 8 — SECURITY

Ensure:

• Only Superadmin role can view system_notifications
• Role-based access enforced
• No sensitive data exposed
• No API keys in frontend
• Validate form input

--------------------------------------------------
STEP 9 — TEST END TO END

Simulate demo booking:

1. Submit form
2. Verify DB insert
3. Verify email received
4. Verify entry in email_logs
5. Verify entry in system_notifications
6. Verify real-time bell notification
7. Verify AI chatbot message appears
8. Verify no console errors

--------------------------------------------------
STEP 10 — FINAL REPORT

After implementation, provide:

1. Confirmation DB insert works
2. Confirmation email service connected
3. Confirmation AI notification system works
4. Confirmation real-time update works
5. Any errors found
6. Production readiness status

Be strict.
Do not assume success.
Show actual log outputs.
